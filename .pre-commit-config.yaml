repos:
  - repo: local
    hooks:
      - id: run-tests
        name: Run tests before push
        entry: bash -c 'if command -v poetry &> /dev/null; then poetry run pytest; else pytest; fi'
        language: system
        stages: [pre-push]
        pass_filenames: false
        always_run: true

